headers = [{"value": field, "text": field} for field in stackings[0]]
items = []
for n,s in enumerate(stackings):
    ss = s.copy()
    ss["id"] = n+1
    items.append(ss)
result = {
    "headers": headers,
    "items": items,
}
